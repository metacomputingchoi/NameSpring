<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:largeHeap="true"
        android:hardwareAccelerated="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.NameSpring"
        tools:targetApi="31" >

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.NameSpring.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ProfileListActivity"
            android:theme="@style/Theme.NameSpring.ProfileList"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.NameSpring.NoActionBar" />

        <activity
            android:name=".NamingActivity"
            android:exported="true"
            android:theme="@style/Theme.NameSpring.NoActionBar" />

        <activity
            android:name=".EvaluationActivity"
            android:exported="true"
            android:theme="@style/Theme.NameSpring.NoActionBar" />

        <activity
            android:name=".CompareActivity"
            android:exported="true"
            android:theme="@style/Theme.NameSpring.NoActionBar" />

        <activity
            android:name=".HistoryActivity"
            android:exported="true"
            android:theme="@style/Theme.NameSpring.NoActionBar" />

        <activity
            android:name=".ProfileFormActivity"
            android:theme="@style/Theme.NameSpring.NoActionBar"
            android:windowSoftInputMode="adjustResize|stateHidden" />

    </application>

</manifest>